<div class="container">
  <div class="row">
    </div><div class="col-xs-12 col-md-offset-3 col-sm-6 padded">
      <div class="simple_form_for">
        <%= simple_form_for [:deltadmin, @user] do |f| %>
          <%= f.input :email %>
          <%= f.input :admin %>
          <%= f.submit "Sauvegarder", class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
